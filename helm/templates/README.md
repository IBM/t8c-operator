# DO NOT rename the templates - they must be ASCII-betical
# Order of operations is important since this contains CRDs
# Reordering the templates will cause errors like "no resource definition found"
